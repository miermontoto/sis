SPOTIFY_CLIENT_ID=your_client_id
SPOTIFY_CLIENT_SECRET=your_client_secret
SPOTIFY_REDIRECT_URI=http://localhost:3000/auth/callback
PORT=3000
DATABASE_PATH=./data/sis.db

# push notifications (opcionales): sin estas vars el pipeline detecta eventos
# pero el sender hace no-op. FIREBASE_SERVICE_ACCOUNT habilita android/FCM;
# las tres VAPID_* habilitan web push (ver docs/PUSH_NOTIFICATIONS_SETUP.md).
FIREBASE_SERVICE_ACCOUNT=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com

# sso propio id.mier.info (opcional): sin estas vars el botón de login no
# aparece. cliente oauth registrado en el issuer; el redirect por defecto es
# <origen de SPOTIFY_REDIRECT_URI>/auth/mierid/callback
MIERID_CLIENT_ID=
MIERID_CLIENT_SECRET=
# MIERID_REDIRECT_URI=
